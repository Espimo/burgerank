// Re-export types from search store for consistency with components
export type { Burger, Restaurant } from '@/lib/stores/search-store'
// Also export from main types directory for other uses
export type { Review, Profile, User, Badge, Reward, UserReward, UserStats } from '@/types'
